{
  "name": "@agent-passport/web",
  "version": "0.1.0",
  "description": "Agent Passport landing page",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.13"
  }
}
