{
  "name": "@agent-passport/extension",
  "version": "0.1.0",
  "description": "Agent Passport Chrome MV3 Extension",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/copy-assets.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-passport/core": "workspace:*",
    "@agent-passport/schema": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.2.13"
  }
}
